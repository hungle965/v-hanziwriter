name: Node.js Package

on:
   push:
    branches:
      - prod

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - run: npm ci
      - run: npm test

  publish-gpr:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: 16
          registry-url: https://npm.pkg.github.com/
      - run: npm ci
      - run: npm run build
      - run: echo "//registry.npmjs.org/:_authToken=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 /n @hungle965:registry=https://npm.pkg.github.com  }}" > ~/.npmrc
      - run: npm publish --access public
        env:
            NODE_AUTH_TOKEN: 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
